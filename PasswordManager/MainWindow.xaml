<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
        Title="PasswordManager" Height="450" Width="800">
    
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Website:" Margin="0,0,0,5"/>
            <TextBox x:Name="WebsiteTextBox" Width="300"/>
            
            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox" Width="300" Margin="0,0,0,15"/>
            
            <TextBlock Text="Password:" Margin="0,0,0,5"/>
            <TextBox x:Name="PasswordTextBox" Width="300" Margin="0,0,0,15"/>
            <Button x:Name="GenerateButton" Content="Generate Password" Width="150" Margin="0,0,0,15" Click="GenerateButton_Click"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Left">
                <Button x:Name="AddButton" Content="Add Password" Width="120" Margin="0,0,10,0" Click="AddButton_Click"/>
                <Button x:Name="DeleteButton" Content="Delete Password" Width="120" Click="DeleteButton_Click"/>
            </StackPanel>

            <TextBlock Text="Saved Passwords:" Margin="0,0,0,5"/>
            <ListView x:Name="PasswordsListView" Height="200" Width="750" SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Website" DisplayMemberBinding="{Binding Website}" Width="200"/>
                        <GridViewColumn Header="Username" DisplayMemberBinding="{Binding Username}" Width="200"/>
                        <GridViewColumn Header="Password" DisplayMemberBinding="{Binding EncryptedPassword}" Width="300"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
