<Window x:Class="PasswordManager.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
        Title="Enter Master Password" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="icon.ico">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Welcome to Password Manager"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Master Password:"
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordBox"
                     VerticalAlignment="Center" Height="25" Margin="0,0,0,10"
                     KeyDown="PasswordBox_KeyDown"/>
        

        <Grid Grid.Row="2" Grid.ColumnSpan="2" x:Name="ConfirmPasswordRow">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Confirm Password:" 
           VerticalAlignment="Center" Margin="0,0,10,10"/>
            <PasswordBox Grid.Column="1" x:Name="ConfirmPasswordBox" 
                 VerticalAlignment="Center" Height="25" Margin="0,0,0,10"
                 KeyDown="PasswordBox_KeyDown"/>
        </Grid>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" 
                   Text="Your master password will be used to encrypt all your stored passwords. Make sure it's strong and you don't forget it." 
                   TextWrapping="Wrap" Margin="0,10,0,10"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" 
                    Width="80" Height="30" Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="LoginButton" Content="Login" 
                    Width="80" Height="30"
                    Click="LoginButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
